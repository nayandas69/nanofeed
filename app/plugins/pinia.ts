/**
 * app/plugins/pinia.ts
 * NanoFeed — Global state initialization
 */

export default defineNuxtPlugin((nuxtApp) => {
  // Pinia is automatically installed & initialized by @pinia/nuxt
  // This file is used if you need to register custom Pinia plugins, e.g. persistence
  // nuxtApp.$pinia.use(myCustomPlugin)
})
